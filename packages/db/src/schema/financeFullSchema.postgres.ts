import { FINANCE_FULL_SCHEMA_SQL } from './financeFullSchema';

/** DDL PostgreSQL — compatible avec le schéma SQLite (TEXT, INTEGER, NUMERIC). */
export const FINANCE_FULL_SCHEMA_POSTGRES_SQL = FINANCE_FULL_SCHEMA_SQL;
